name: Handle show start
on:
  schedule:
    - cron: 30 19 18 6 *
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: node create-comment-on-current-show.js
        env:
          GITHUB_TOKEN: ${{ secrets.secrets.GR2M_PAT }}
